# BlackICE-Mesh shared infrastructure credentials
# Copy to .env and adjust before running: docker compose up

RABBITMQ_USER=blackice
RABBITMQ_PASS=blackice
POSTGRES_USER=blackice
POSTGRES_PASS=blackice
POSTGRES_DB=blackice
